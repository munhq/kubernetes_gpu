# Prometheus Stack Helm values
# Monitoring with Prometheus, Grafana, AlertManager

# Grafana configuration
grafana:
  enabled: true
  adminPassword: admin  # TODO: Change this password!

  # Disable legacy init container - fsGroup in securityContext handles permissions
  # The init container is incompatible with K3s local-path provisioner
  initChownData:
    enabled: false

  # Persist Grafana dashboards
  persistence:
    enabled: true
    size: 10Gi

  # Resource limits
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

  # Install Grafana plugins
  plugins:
    - yesoreyeram-infinity-datasource

  # Additional datasources beyond Prometheus
  additionalDataSources:
    - name: GPU-API
      type: yesoreyeram-infinity-datasource
      access: proxy
      url: http://gpu-api.gpu-workloads.svc.cluster.local:8000
      isDefault: false
      editable: true

  # Disable ALL default dashboards - user will add what they need manually
  defaultDashboardsEnabled: false

  # Sidecar auto-discovers ConfigMaps with label grafana_dashboard=1
  # Folder provisioning is handled by custom providers, not sidecar
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: ALL
      provider:
        disableDelete: false
        foldersFromFilesStructure: false
        folder: ''
        # Disable sidecar's own dashboard provider - we use custom providers for folders
        disable: true

  # Dashboard providers - separate provider for each folder
  dashboardProviders:
    dashboardproviders.yaml:
      apiVersion: 1
      providers:
        - name: 'custom-dashboards'
          orgId: 1
          folder: 'Custom'
          type: file
          disableDeletion: false
          editable: true
          options:
            path: /tmp/dashboards/Custom
        - name: 'ray-dashboards'
          orgId: 1
          folder: 'Ray'
          type: file
          disableDeletion: false
          editable: true
          options:
            path: /tmp/dashboards/Ray

  # Community dashboards from grafana.com
  # User will add dashboards manually via Grafana UI
  dashboards: {}

# Prometheus configuration
prometheus:
  prometheusSpec:
    # Retention period
    retention: 30d

    # Storage
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi

    # Resource limits
    resources:
      requests:
        cpu: 500m
        memory: 2Gi
      limits:
        cpu: 2000m
        memory: 4Gi

    # Service monitors - automatically discover services
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false

# AlertManager configuration
alertmanager:
  enabled: true
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi

# Node exporter for host metrics
nodeExporter:
  enabled: true

# Kube-state-metrics for Kubernetes metrics
kubeStateMetrics:
  enabled: true

# Default rules
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: false  # K3s doesn't expose etcd metrics
    general: true
    k8s: true
    kubeApiserver: true
    kubeApiserverAvailability: true
    kubeApiserverSlos: true
    kubelet: true
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeScheduler: false  # K3s doesn't expose scheduler metrics
    kubeStateMetrics: true
    network: true
    node: true
    prometheus: true
    prometheusOperator: true
