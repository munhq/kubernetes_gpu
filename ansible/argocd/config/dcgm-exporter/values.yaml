# NVIDIA DCGM Exporter configuration
# Exports GPU metrics to Prometheus

serviceMonitor:
  enabled: true

# Force dcgm-exporter to run ONLY on GPU nodes
nodeSelector:
  nvidia.com/gpu: present

tolerations:
  - key: nvidia.com/gpu
    operator: Exists
    effect: NoSchedule
