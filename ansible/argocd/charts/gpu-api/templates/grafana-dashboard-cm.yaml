apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gpu-api.fullname" . }}-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
    app: {{ include "gpu-api.name" . }}
data:
  gpu-api.json: |-
{{ .Files.Get "dashboards/gpu-api.json" | indent 4 }}
