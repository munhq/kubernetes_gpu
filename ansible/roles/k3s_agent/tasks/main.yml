#SPDX-License-Identifier: MIT-0
---
# tasks file for k3s_agent

- name: Get current Netbird VPN IP
  ansible.builtin.shell: |
    netbird status | grep -oP 'NetBird IP:\s*\K[\d.]+'
  register: vpn_ip_raw
  changed_when: false

- name: Store Netbird VPN IP as fact
  ansible.builtin.set_fact:
    netbird_vpn_ip: "{{ vpn_ip_raw.stdout | trim }}"
    netbird_vpn_ip_changed: "{{ (netbird_vpn_ip is defined and netbird_vpn_ip != vpn_ip_raw.stdout | trim) | bool }}"

- name: Display NetBird IP info
  ansible.builtin.debug:
    msg: "NetBird VPN IP: {{ netbird_vpn_ip }}{{ ' (CHANGED - will trigger restart)' if netbird_vpn_ip_changed else '' }}"

- name: Restart NetBird if IP changed
  ansible.builtin.systemd:
    name: netbird
    state: restarted
  when: netbird_vpn_ip_changed | default(false)

- name: Wait for NetBird to reconnect after restart
  ansible.builtin.pause:
    seconds: 10
  when: netbird_vpn_ip_changed | default(false)

- name: Verify NetBird connectivity to server
  ansible.builtin.shell: |
    # Get server IP and verify connectivity
    ping -c 3 -W 5 {{ hostvars[groups['k3s_server'][0]]['netbird_vpn_ip'] | default('100.97.107.135') }}
  register: netbird_server_ping
  changed_when: false
  retries: 3
  delay: 5
  until: netbird_server_ping.rc == 0

- name: Get K3s server URL from server host
  ansible.builtin.set_fact:
    k3s_server_url: "{{ hostvars[groups['k3s_server'][0]]['k3s_server_url'] }}"
    k3s_token: "{{ hostvars[groups['k3s_server'][0]]['k3s_token'] }}"

- name: Check if K3s agent is installed
  ansible.builtin.stat:
    path: /usr/local/bin/k3s
  register: k3s_binary

- name: Download K3s install script
  ansible.builtin.get_url:
    url: https://get.k3s.io
    dest: /tmp/k3s-install.sh
    mode: "0755"

- name: Install K3s agent with node-ip argument
  ansible.builtin.shell: |
    INSTALL_K3S_VERSION={{ k3s_version }} \
    K3S_URL={{ k3s_server_url }} \
    K3S_TOKEN={{ k3s_token }} \
    INSTALL_K3S_EXEC="agent --node-name={{ inventory_hostname }} --node-ip={{ netbird_vpn_ip }} --flannel-iface=wt0" \
    /tmp/k3s-install.sh
  register: k3s_agent_install
  changed_when: "'No change detected' not in k3s_agent_install.stderr"

- name: Ensure K3s agent service is enabled and started
  ansible.builtin.systemd:
    name: k3s-agent
    enabled: true
    state: started
  when: k3s_agent_install is changed

- name: Wait for K3s CNI to initialize
  ansible.builtin.wait_for:
    path: /var/lib/rancher/k3s/agent/etc/cni/net.d/10-flannel.conflist
    timeout: 120

- name: Wait for K3s agent service to become active
  ansible.builtin.systemd:
    name: k3s-agent
    state: started
  register: k3s_agent_service
  until: k3s_agent_service.status.ActiveState == "active"
  retries: 30
  delay: 10

- name: Wait for node to register with server
  ansible.builtin.command: >
    kubectl get node {{ inventory_hostname }}
  delegate_to: "{{ groups['k3s_server'][0] }}"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  register: node_check
  until: node_check.rc == 0
  retries: 20
  delay: 5
  changed_when: false

- name: Apply node labels (from server)
  ansible.builtin.command: >
    kubectl label node {{ inventory_hostname }}
    {{ item.key }}={{ item.value }}
    --overwrite
  delegate_to: "{{ groups['k3s_server'][0] }}"
  loop: "{{ k3s_node_labels | default({}) | dict2items }}"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  changed_when: true
  when: k3s_node_labels is defined

- name: Apply node taints (from server)
  ansible.builtin.command: >
    kubectl taint node {{ inventory_hostname }}
    {{ item }}
    --overwrite
  delegate_to: "{{ groups['k3s_server'][0] }}"
  loop: "{{ k3s_node_taints | default([]) }}"
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  changed_when: true
  failed_when: false
  when: k3s_node_taints is defined

- name: Display agent info
  ansible.builtin.debug:
    msg: |
      K3s Agent installed successfully!
      Connected to: {{ k3s_server_url }}
      Node name: {{ inventory_hostname }}
      Node labels: {{ k3s_node_labels | default({}) }}
      Node taints: {{ k3s_node_taints | default([]) }}
