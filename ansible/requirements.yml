---
# Collections the roles and plays depend on. Install with:
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: community.general
  - name: kubernetes.core
  - name: ansible.posix
  - name: community.github
